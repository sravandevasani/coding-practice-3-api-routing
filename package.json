{
  "name": "api-routing",
  "version": "1.0.0",
  "description": "Given an `app.js` file, write two APIs that sends different strings as responses.",
  "main": "app.js",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "4.17.1"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": []
}
